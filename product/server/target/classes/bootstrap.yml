spring:
  application:
    name: product
  cloud:
    config:
      profile: dev
      discovery:
        service-id: CONFIG
        enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/